# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=compliance_db
DB_USER=postgres
DB_PASSWORD=postgres

# LLM (Phase 2 이후)
LLM_PROVIDER=openai
LLM_API_KEY=

# Spring 프로파일 (local | prod)
SPRING_PROFILES_ACTIVE=local
