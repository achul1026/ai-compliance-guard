# 🗺️ ROADMAP — ai-compliance-guard

> AI 기반 건기식/뷰티 광고 심의 컴플라이언스 검사기 개발 로드맵
> 상세 기획은 [AI_Compliance_Auditor_Plan.md](AI_Compliance_Auditor_Plan.md) 참고.
> 서비스 운영은 [OPERATIONS.md](OPERATIONS.md), 시장 전략은 [STRATEGY.md](STRATEGY.md) 참고.

**타겟 도메인**: 건강기능식품/화장품 SNS 광고 카피 심의 위반 검사
**진행 방식**: 단계별로 하나씩 발전. 각 단계는 검증 기준을 통과해야 다음으로 넘어간다.

---

## 📌 진행 현황

| Phase | 내용 | 상태 |
| :--- | :--- | :--- |
| Phase 0 | 프로젝트 기반 셋업 | ✅ 완료 (2026-05-26) |
| Phase 1 | 데이터 구축 + Hybrid RAG | ✅ 완료 (2026-06-07, Recall@10 law_only 88%) |
| Phase 2 | Multi-Agent 파이프라인 + UI | ✅ 완료 (2026-06-13, 3-Agent + 심의소 UI + 디자인 고도화) |
| Phase 3 | 엔터프라이즈 보안 + 수익화 | 🟡 인증·암호화·사용량·법적고지·보안·랜딩·배포 완료, 결제(P3-4)만 사업자등록 대기 |

상태 표기: ⬜ 대기 · 🟡 진행 중 · ✅ 완료

---

## 🔑 선행 기술 결정 사항 (Phase 0~1 진입 전 확정)

아래 항목은 구현 도중 번복하면 비용이 큰 결정들이다. 해당 단계 진입 시 함께 논의해 확정한다.

- **D1. Gradle 모듈 구조**: 초기에는 단순하게 시작하고 필요 시 분할 (Phase 0에서 확정).
- **D2. 한국어 BM25 구현 방식**: PostgreSQL 기본 full-text search는 진정한 BM25가 아니고 한국어 형태소 분석이 약함. 후보 — (a) `pg_search`(ParadeDB) 확장, (b) 별도 검색엔진(OpenSearch/Elasticsearch + nori), (c) 애플리케이션 레벨 BM25(Lucene). Phase 1에서 확정.
- **D3. 임베딩 모델**: 한국어 법령/규정 특화 필요. 후보 — OpenAI text-embedding-3, Upstage solar-embedding, BGE-m3 등. Phase 1에서 확정.
- **D4. Re-ranking 방식**: Cohere Rerank, Upstage, cross-encoder 등. Phase 1에서 확정.
- **D5. LLM 엔진**: ✅ 확정 (2026-06-13, ADR-006) — **Gemini Flash 무료 티어** (`LlmPort` 격리, 실고객 데이터 수신 시점에 유료 전환). 원안 후보(GPT-4o/Upstage Solar)는 "수익 전 비용 0" 원칙으로 탈락.
- **D6. 에이전트 오케스트레이션**: ✅ 확정 (2026-06-13, ADR-007) — **LangChain4j(LLM 호출 계층) + 자체 상태머신(:agent 모듈)**. 그래프 프레임워크는 현 복잡도에 과잉으로 미도입.

---

## 📅 Phase 0 — 프로젝트 기반 셋업

> 목표: 코드를 작성할 수 있는 빌드/실행 환경을 갖춘다.

- [ ] **P0-1. Gradle 멀티모듈 골격 생성**
  초기 모듈안: `:api`(Spring Boot 웹/REST), `:agent`(에이전트 파이프라인), `:rag`(검색), `:infra`(영속성·외부 API), `:common`(공용 DTO/유틸).
  → 검증: `./gradlew build` 성공.
- [ ] **P0-2. Spring Boot 애플리케이션 부트스트랩**
  `:api` 모듈에 `@SpringBootApplication`, 헬스체크 엔드포인트(`GET /health`) 추가.
  → 검증: 앱 기동 후 `/health`가 200 응답.
- [ ] **P0-3. 로컬 인프라 구성 (Docker Compose)**
  PostgreSQL + pgvector 확장 컨테이너 정의.
  → 검증: `docker compose up` 후 DB 접속 + `CREATE EXTENSION vector` 성공.
- [ ] **P0-4. 설정·시크릿 관리 체계**
  `application.yml` 프로파일 분리(local/prod), LLM API 키 등 환경변수 주입 구조. `.gitignore` 정비.
  → 검증: 로컬 프로파일로 앱이 DB에 연결됨.

---

## 📅 Phase 1 — 데이터 구축 + Hybrid RAG (1~2개월)

> 목표: "이 광고 문구가 위반되는가?"라는 질의에 정확한 근거 조항을 찾아오는 검색 API를 완성한다.

### 1-A. 규정 데이터 수집·정제
- [ ] **P1-1. 근거 규정 소스 확정 및 수집**
  대상: 식품 등의 표시·광고에 관한 법률(시행령/시행규칙 포함), 건강기능식품에 관한 법률, 화장품법, 식약처 표시·광고 가이드라인, 부당 표시·광고 FAQ, 협회 자율심의 기준.
  → 검증: 원문 PDF/문서 목록과 출처가 정리됨.
- [ ] **P1-2. PDF 파싱 및 청킹 전략 수립**
  조(條)·항(項)·호(號) 단위 구조를 보존하는 청킹. 메타데이터(법령명, 조항번호, 시행일) 부착.
  → 검증: 샘플 법령이 조항 단위로 올바르게 분할되고 메타데이터가 채워짐.
- [ ] **P1-3. 위반 유형 택소노미 정의**
  예: 질병 예방·치료 효능 표방, 의약품 오인, 거짓·과장, 객관적 근거 없는 표현, 소비자 기만 등 분류 체계.
  → 검증: 위반 유형별로 대표 예시·근거 조항이 매핑된 표 완성.

### 1-B. Hybrid RAG 파이프라인
- [ ] **P1-4. DB 스키마 설계**
  규정 청크 테이블(본문, 메타데이터, 임베딩 vector 컬럼), 인덱스 설계.
  → 검증: 마이그레이션 적용 + 샘플 데이터 적재 성공.
- [ ] **P1-5. 임베딩 파이프라인 (D3 확정)**
  청크 → 임베딩 생성 → pgvector 적재 배치 작업.
  → 검증: 전체 청크가 임베딩되어 벡터 검색 쿼리가 동작.
- [ ] **P1-6. 키워드 검색(BM25) 구현 (D2 확정)**
  조항 번호·고유명사 매칭용 키워드 검색.
  → 검증: "식품표시광고법 제8조" 같은 질의가 정확히 해당 조항을 반환.
- [ ] **P1-7. 하이브리드 결합 + Re-ranking (D4 확정)**
  벡터 + 키워드 결과 병합 후 관련도 재정렬.
  → 검증: 테스트 질의 세트에서 정답 조항이 상위 N개 안에 포함.
- [ ] **P1-8. 검색 API 완성**
  `POST /api/search` — 질의 입력 → 관련 조항 컨텍스트 반환.
  → 검증: API 통합 테스트 통과, 검색 정확도 평가 세트로 측정.

---

## 📅 Phase 2 — Multi-Agent 파이프라인 + UI (1개월)

> 목표: 광고 문구를 입력하면 위험 구절 하이라이팅 + 대안 제시 리포트를 출력한다.

### 2-A. 에이전트 오케스트레이션
- [x] **P2-1. 오케스트레이션 프레임워크 도입 (D5·D6 확정)**
  → 검증: 단순 단일 에이전트 호출이 엔드투엔드로 동작.
- [x] **P2-2. 검사관(Auditor) 에이전트**
  검색 컨텍스트 기반으로 위반 소지·리스크 리포트 생성.
  → 검증: 위반 샘플 입력 시 위반 구절·근거 조항·위험도가 출력.
- [x] **P2-3. 비판관(Critique) 에이전트**
  Auditor가 인용한 근거 법령의 실재성·정합성 교차 검증(환각 차단).
  → 검증: 의도적으로 잘못된 근거를 심으면 Critique가 탐지.
- [x] **P2-4. 교정관(Remediator) 에이전트**
  안전한 대체 문구 제안.
  → 검증: 위반 구절마다 규정에 맞는 대안 문구가 생성.
- [x] **P2-5. Stateful 워크플로우 + 순환 루프**
  Critique가 오류 감지 시 Auditor로 피드백하는 루프 제어, 무한루프 방지 한계 설정.
  → 검증: 피드백 루프가 동작하고 최대 반복 횟수에서 안전하게 종료.
- [x] **P2-6. 분석 API 완성**
  `POST /api/audit` — 광고 문구 입력 → 최종 컴플라이언스 리포트.
  → 검증: 엔드투엔드 통합 테스트 통과.

### 2-B. 프론트엔드 (Phase 2.5, 2026-06-13)
- [x] **P2-7. 입력 UI**
  심의소 단일 페이지(`static/index.html`, 앱인토스 WebView 규격 모바일 우선). 광고 카피 입력 → `POST /api/v1/audit`.
  → 검증: ✅ 브라우저 E2E로 입력→분석 동작.
- [x] **P2-8. 결과 대시보드**
  위험 구절 하이라이팅(위험도별 색 밑줄), 위반 카드(근거 조항·사유·대체 문구 탭-복사), 판정 도장.
  → 검증: ✅ 실 파이프라인으로 부적합 판정·위반 렌더링 확인.
- [x] **P2-9. 디자인 고도화** (2026-06-13)
  AI 클리셰(컬러 보더 callout·이모지·파란 강조색·강제 줄바꿈) 제거 → 전통 공문서·심의서 미학(괘선·인장·명조)으로 정제. 첫 화면 히어로 카피 강화.

---

## 📅 Phase 3 — 엔터프라이즈 보안 + 수익화 (1개월)

> 목표: B2B 고객이 안심하고 결제해 쓸 수 있는 서비스로 런칭한다.

- [x] **P3-1. 인증/세션 보안** (2026-06-13, ADR-008)
  Spring Security + BCrypt + JWT 이중 토큰(Access 15분 / Refresh 14일, HttpOnly 쿠키, 회전·서버측 무효화). V6(users, refresh_tokens). 엔드포인트: signup/login/logout/refresh/me. `/admin/**`는 ADMIN role 보호로 전환.
  → 검증: ✅ 자동 테스트 14건(JWT 단위 6 + 시큐리티 슬라이스 8) + 실 DB 풀 플로우 E2E(회원가입→로그인→me→회전→옛 토큰 폐기→로그아웃 무효화, 데모 무손상). 분석 이력은 P3-3로 이연.
- [x] **P3-2. 데이터 프라이버시 강화** (2026-06-13, ADR-009)
  분석 이력(`audit_history`) AES-256-GCM 투명 암호화(JPA AttributeConverter, `enc1:` 포맷, 레거시 평문 호환). PII 마스킹 유틸(이메일·전화·주민번호, 로그용). LLM 학습거부는 무료 티어 불가 → "무료 티어=실고객 금지" OPERATIONS 명문화 + opt-out 설정 자리. 분석 이력 조회 API(`GET /audit/history`, 복호화).
  → 검증: ✅ 암호화 단위 7 + 마스킹 5 + 실 DB 라운드트립(원시조회 `enc1:` 암호문·평문노출 0 → 앱조회 복호 평문). 잔여 리스크: 무료 티어 LLM 전송분은 정책으로만 방어(유료 전환 시 종결).
- [x] **P3-3. 사용량 제한 (Free Tier)** (2026-06-13)
  로그인 Free Tier 월 5회(`app.usage.free-monthly-limit`). V7(audit_history=이력+카운터). `/audit` 로그인 필수 전환, 한도 초과 시 429. `GET /audit/usage` 잔여 조회. UI: 로그인 게이트 모달(가입/로그인) + 잔여 횟수 배지 + 429 안내.
  → 검증: ✅ 단위/슬라이스 테스트 + 실 DB E2E(미로그인 401 → 한도 내 200·잔여 감소 → 초과 429) + 브라우저 E2E(가입→심의→배지 2→1→부적합 렌더). 비로그인 무료 체험은 미도입(전환율 보고 추후).
- [ ] **P3-4. 결제 연동 (이중 채널)**
  STRATEGY §5 티어 구조에 맞춘 두 결제 경로 동시 구현. 공통 구독·크레딧 도메인 모델 위에 결제 어댑터를 분리한다.
  - **P3-4a. B2B 결제 (Pro / Enterprise)**: 토스페이먼츠 카드 결제 — 월간 구독 + 건당 크레딧 패키지.
    → 검증: 카드 결제 성공, 구독 상태·크레딧 잔액 반영, 영수증 발급.
  - **P3-4b. B2C 결제 (Lite)**: 토스페이 빌링키 — 토스인앱 진입 사용자의 정기결제 자동 결제.
    → 검증: 빌링키 발급 + 정기결제 실행 성공, 정기결제 사전 고지·청약철회 7일 UI 노출, 해지 플로우 동작.
- [ ] **P3-5. 런칭 준비 (이중 채널)** — 🟡 일부 완료 (2026-06-13)
  - [x] **P3-5a. B2B 랜딩** (2026-06-13): `landing.html` — 히어로 후크+CTA, 통계 띠, 위험 표현 칩, 작동 방식 3단계, 핵심 가치, 가격표(Free/Lite/Pro), "유출되지 않습니다" 보안 배너, 최종 CTA. 라임 플랫 디자인, 모바일 우선+반응형. (문의 폼은 도입 문의 링크로 대체, 정식 폼은 추후)
  - **P3-5b. 토스인앱 진입 페이지**: 모바일 우선 반응형, WebView UA 분기, 외부 OAuth 회피(이메일/자체 인증), 토스인앱 입점 신청 병행. 🟡 (심의소 UI가 모바일 우선·자체 인증·라임 플랫으로 기반 충족, 입점 신청은 등록증 후)
  - **P3-5c. 공통**:
    - [x] **약관·개인정보처리방침·면책 고지** (2026-06-13): `legal.html`(이용약관/개인정보처리방침/면책, P3-2 데이터 정책·외부 LLM 고지 반영), index footer 링크 + 회원가입 약관 동의. ⚠️ 법무 검토 전 초안.
    - [x] **운영 보안 점검** (2026-06-13): prod 시크릿 fail-fast, `JWT_COOKIE_SECURE=true`, 보안 헤더(nosniff/SAMEORIGIN/HSTS), `_workspace/PROD_SECURITY_CHECKLIST.md`.
    - [x] **배포 파이프라인** (2026-06-13): Dockerfile(멀티스테이지 gradle→JRE, 비루트, 헬스체크, `docker build` 검증 485MB), `.dockerignore`, docker-compose `app`/`reranker` 프로파일, `DEPLOY.md`. CI·모니터링·클라우드 배포는 타겟 확정 후.
    - [ ] 통신판매업: 간이과세자 신고 면제(STRATEGY §6), PG 심사 시 요구되면 신고.
  → 검증: 운영 환경 배포 후 B2B·B2C 양 채널 핵심 플로우(가입→결제→분석→리포트) 동작 확인.

> **부가 작업 (2026-06-13)** — Phase 3 트랙과 함께 진행:
> - 디자인 리뉴얼: 전통 공문서 → **라임 그린 플랫 컨셉** (AI 클리셰 제거, 3페이지 통일). 한국어 어절 단위 줄바꿈.
> - "내 심의 이력" UI: `GET /audit/history` 연결, 복호화 카피·위험도·날짜 목록.
> - 품질 트랙: **BGE-reranker-v2-m3 로컬 어댑터** 재구현(provider 토글, 비용 0). 실 TEI 컨테이너 실측·strict recall 개선·가중치 튜닝은 출시 후 트랙.

---

## 다음 액션

**Phase 3 마무리 — 결제(P3-4)만 남음.** 사업자등록증 발급 후 토스페이먼츠 연동으로 진입한다.

등록증 대기 중 진행 가능한 잔여 작업:
- **P3-5a. B2B 랜딩 페이지** — 가격표·보안 마케팅·문의 폼 (등록증 무관)
- **품질 트랙** — BGE-reranker 로컬 전환, 검색 strict recall 개선, 검색 API E2E 테스트 (출시 전)
- **배포 파이프라인** — Dockerfile·CI·모니터링 (클라우드 타겟 확정 후)

현재 동작: 회원가입 → 로그인 → 광고 카피 입력 → AI 3-Agent 심의 → 위반·근거·대체문구 리포트 → 분석 이력 암호화 저장. 무료 월 5회.
